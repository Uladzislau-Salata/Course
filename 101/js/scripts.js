// 'use strict';

import { one, two } from './mains.js';
// import { one as first } from './mains';
// import * as data from './mains';
import sayHi from './mains.js';

// console.log(first);
console.log(`${one} and ${two}`);

// console.log(`${data.one} and ${data.two}`);
// data.sayHi();
sayHi();